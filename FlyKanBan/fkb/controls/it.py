
def get_data(it_id:int, **kvargs):

    # load all itors (different versions of the same it)

    # load all storys, tasks, bugs of current itor.

    # calculate all datas of current itor.

    return {
        'it_id':it_id,
        'its':[],
        
        }